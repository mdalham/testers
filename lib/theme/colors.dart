import 'package:flutter/material.dart';

// ─────────────────────────────────────────────
//  Light Theme Colors
// ─────────────────────────────────────────────

/// Background / scaffold
const Color bgLight = Color(0xFFFFFFFF);

/// Primary text color
const Color fontLight = Color(0xFF202124);

/// Secondary / subtitle text color
const Color subFontLight = Color(0xFF5F6368);

/// Card / container background
const Color containerLight = Color(0xFFF1F3F4);

/// Foreground surface (slightly off-white layer above scaffold)
const Color forGroundLight = Color(0xFFE8F0FE);

/// Border / divider
const Color outlineLight = Color(0xFFDADCE0);

/// Subtle outline for inner containers / chips
const Color forGroundOutlineLight = Color(0xFFC4C7CC);

/// Icon background / tinted icon surface
const Color lightIconBackgroundColor = Color(0x80ECEFF1);

// ─────────────────────────────────────────────
//  Dark Theme Colors
// ─────────────────────────────────────────────

/// Background / scaffold
const Color bgDark = Color(0xFF1E1E1E);

/// Primary text color
const Color fontDark = Color(0xFFE8EAED);

/// Secondary / subtitle text color
const Color subFontDark = Color(0xFF9AA0A6);

/// Card / container background
const Color containerDark = Color(0xFF2D2F31);

/// Foreground surface (slightly elevated layer above scaffold)
const Color forGroundDark = Color(0xFF3C4043);

/// Border / divider
const Color outlineDark = Color(0xFF5F6368);

/// Subtle outline for inner containers / chips
const Color forGroundOutlineDark = Color(0xFF444749);

/// Icon background / tinted icon surface
const Color darkIconBackgroundColor = Color(0x802A2D2F);

// ─────────────────────────────────────────────
//  Chrome-style Dark Accent
// ─────────────────────────────────────────────

/// Used for app bars or prominent dark surfaces (Chrome dark gray)
const Color darkAccent = Color(0xFF202124);

// ─────────────────────────────────────────────
//  Semantic / Extra Colors (shared across themes)
// ─────────────────────────────────────────────

const Color blue   = Color(0xFF1A73E8);
const Color green  = Color(0xFF34A853);
const Color orange = Color(0xFFF29900);
const Color red    = Color(0xFFD93025); //0xFFB71C1C
const Color yellow = Color(0xFFFBBC04);

// Dark variants of the semantic colors
const Color blueDark   = Color(0xFF8AB4F8);
const Color greenDark  = Color(0xFF81C995);
const Color orangeDark = Color(0xFFFDD663);
const Color redDark    = Color(0xFFF28B82);
const Color yellowDark = Color(0xFFFDE68A);